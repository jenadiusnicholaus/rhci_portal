from django.urls import path
from . import views

urlpatterns = [
    # Define your referral system URLs here
    # path('refer/', views.create_referral, name='create_referral'),
    # path('track/<str:code>/', views.track_referral, name='track_referral'),
]