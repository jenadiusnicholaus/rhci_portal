from django.contrib import admin

# Restore default admin site (no custom dashboard, no metrics)
# If you previously replaced admin.site, remove that line as well.

# Just leave this file as:
from django.contrib import admin
